/**
 * Created by Administrator on 2018/4/2 0002.
 *//*
$.post("http://10.10.1.219:8080/yuesao/taxonomy/list",function (result) {
    if(result.code=="USR000"){
        var classifys=resilt.data;
        console.log("classify:"+classifys);

    }else if(result.code=="USR108"){
        layer.open({
            content: result.msg
            ,btn: ['确定',],
            skin:'my-skin'
            ,yes: function(index){
                layer.close(index);
            }
        });
    }else if(result.code=="USR201"){
        layer.open({
            content: result.msg
            ,btn: ['确定',],
            skin:'my-skin'
            ,yes: function(index){
                layer.close(index);
            }
        });
    }
});*/